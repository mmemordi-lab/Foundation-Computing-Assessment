

//ASSESSMENT 5 GRADE D 
#include <stdio.h>

int main()
{
    int numberOfDays;
    int day;
    int menuChoice;

    float temperatures[30];
    float totalTemperature;
    float averageTemperature;

    printf("\n  ======== SOL ASSESSMENT 5 ========\n");
    printf("  === DAILY TEMPERATURE LOGGER  ===\n");

    printf("      ======= GRADE D =======\n\n");

   // ASK USER TO ENTER NUMBER OF DAYS

    do
    {
        printf("How many days do you want to record (1-30): ");
        scanf("%d", &numberOfDays);

        if(numberOfDays < 1 || numberOfDays > 30)
        {
            printf("Invalid number of days!\n");
            printf("Please enter a number between 1 and 30.\n\n");
        }

    }
    while(numberOfDays < 1 || numberOfDays > 30);

    //    TEMPERATURE READINGS

    for(day = 0; day < numberOfDays; day++)
    {
        printf("Enter temperature for Day %d: ",
               day + 1);

        scanf("%f", &temperatures[day]);
    }


    //    MENU COMES HERE 
   
    do
    {
        printf("\n=========================================\n");
        printf("              TEMPERATURE MENU\n");
        printf("=========================================\n");

        printf("1. Display all temperature readings\n");
        printf("2. Calculate average temperature\n");
        printf("3. Exit\n");

        printf("\nEnter your choice: ");
        scanf("%d", &menuChoice);


        switch(menuChoice)
        {

               // OPTION 1:  DISPLAY READINGS

            case 1:
                printf("\n=========================================\n");
                printf("        TEMPERATURE READINGS\n");
                printf("=========================================\n");

                for(day = 0; day < numberOfDays; day++)
                {
                    printf("Day %d: %.2f C\n",
                           day + 1,
                           temperatures[day]);
                }

                break;


         //       OPTION 2: CALCULATE AVERAGE

            case 2:

                totalTemperature = 0;

                for(day = 0; day < numberOfDays; day++)
                {
                    totalTemperature =
                    totalTemperature + temperatures[day];
                }

                averageTemperature =
                totalTemperature / numberOfDays;

                printf("\nAverage Temperature: %.2f C\n",
                       averageTemperature);

                break;


            //    OPTION 3:  EXIT

            case 3:

                printf("\nThank you for using the\n");
                printf("Daily Temperature Logger!\n");

                break;


         //      INVALID MENU CHOICE

            default:

                printf("\nInvalid choice. Please try again.\n");
        }

    }
    while(menuChoice != 3);


    return 0;
}




//ANALYSIS FOR GRADE D 
//Grade D requires we
// 1. Ask users how many days to record (up to 30)
// 2. Store the temperatures in an array
// 3. Provide a menu for displaying all readings
// 4. Calculate the overall average.
